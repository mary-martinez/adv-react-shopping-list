import { render } from '@testing-library/react';
import App from './App';

describe('App', () => {
  it.skip('should load a header, an input, and an initial list of items', () => {
    render(<App />);
  });
});
